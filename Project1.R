#1. Compute standard descriptive statistics for the variables in this study
wages <- bwages["WAGE"][1]
wages[1]
wages[2]
wages_num <- as.numeric(as.vector((wages)))
sd_stats <- matrix((mean(wages), median(wages), sd(wages), var(wages))

                   
                   
                  
apply(bwages,mean)
